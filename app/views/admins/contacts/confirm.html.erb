<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3">
    <br>
    <h3>お問い合わせ返信内容確認</h3>
    <br>
    <h5>本当に送信しますか？</h5>
    <br>
    <table>
      <tbody>
        <tr>
          <td class="text-center" style="width: 30%;">タイトル：</td>
          <td><%= @contact.subject %></td>
        </tr>
        <tr>
          <td class="text-center">メッセージ：</td>
          <td style="white-space: pre-wrap;"><%= @contact.message %></td>
        </tr>
        <tr>
          <td class="text-center">送信先：</td>
          <td style="white-space: pre-wrap;"><%= @contact.customer_id %></td>
        </tr>
      </tbody>
    </table>
    <br>
    <%= form_for @contact, url: admins_contacts_path do |f| %>
      <%= f.hidden_field :customer_id %>
      <%= f.hidden_field :subject %>
      <%= f.hidden_field :message %>
      <%= f.submit '送信', class: "btn btn-success" %>
    <% end %>
    
    <%= form_for @contact, url: admins_contacts_back_path do |f| %>
      <%= f.hidden_field :customer_id %>
      <%= f.hidden_field :subject %>
      <%= f.hidden_field :message %>
      <%= f.submit '入力画面に戻る', class: "btn btn-success" %>
    <% end %>
   
    </div>
  </div>
</div>